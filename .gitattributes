# Large UGS game bundles — store with Git LFS (install: https://git-lfs.com)
# After clone: run `git lfs install` and `git lfs pull` if files show as pointers.
Client/UGS Files/** filter=lfs diff=lfs merge=lfs -text
